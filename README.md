# 🤖 Telegram UserBot

Многофункциональный юзербот на Telethon с RP-командами, загрузкой медиа (YouTube / Instagram / TikTok) и стелс-функциями.

## 🚀 Запуск

```bash
pip install -r requirements.txt
python main.py
```

### Переменные окружения

| Переменная | Описание |
|-----------|----------|
| `API_ID` | API ID из my.telegram.org |
| `API_HASH` | API Hash оттуда же |
| `STRING_SESSION` | Строка сессии Telethon |
| `OWNER_ID` | Telegram ID владельца (по умолч. `5457847440`) |
| `HIBP_API_KEY` | Для Have I Been Pwned (опционально) |
| `PORT` | Порт Flask-статуса (по умолч. `8080`) |
| `MEDIA_DIR` | Директория для скачанных файлов (по умолч. `./media`) |

## 📋 Команды

### Стелс / Приватность
| Команда | Описание |
|---------|----------|
| `!typing on/off` | Вкл/выкл индикатор печати |
| `!autodel on/off [сек]` | Автоудаление исходящих сообщений |
| `!delay <сек>` | Задержка перед автоудалением |
| `!readreceipt on/off` | Вкл/выкл уведомления о прочтении |
| `!silent on/off` | Тихий режим (без звука) |

### Управление
| Команда | Описание |
|---------|----------|
| `!help [all]` | Справка по командам |
| `!status` | Статус бота |
| `!sudo on/off @user` | Добавить/удалить sudo-пользователя |
| `!resetdata` | Сброс всех настроек |
| `!lock` / `!unlock` | Заблокировать/разблокировать бота |
| `!block @user` | Заблокировать пользователя |
| `!unblock @user` | Разблокировать пользователя |

### Загрузка медиа
| Команда | Описание |
|---------|----------|
| `!dl <url>` | Скачать видео (YouTube / Instagram / TikTok) |
| `!audio <url>` | Скачать аудио (YouTube) |
| `!ytshow <url>` | Информация о YouTube-видео |
| `!patch <url>` | Скачать из источника |
| `!playlist <url>` | Скачать плейлист |
| `!sub <url> [lang]` | Скачать субтитры (умолч. ru) |
| `!stop` | Остановить текущую загрузку |

### Генерация изображений
| Команда | Описание |
|---------|----------|
| `!color <hex>` | Цветное изображение по hex |
| `!img <текст>` | Изображение с текстом |
| `!imgr <текст>` | Изображение с повёрнутым текстом |
| `!q <текст>` | Цитата-изображение |
| `!meme <верх \| низ>` | Мем с подписями |

### Утилиты
| Команда | Описание |
|---------|----------|
| `!id` | Мой ID |
| `!info [@user]` | Информация о пользователе |
| `!ping` | Пинг |
| `!spam <количество> <текст>` | Спам сообщениями (макс. 50) |
| `!base64 encode/decode <текст>` | Base64 кодирование |
| `!save <текст>` | Сохранить данные |
| `!saved` | Список сохранённого |
| `!del <id>` | Удалить сохранённое |
| `!stats` | Статистика бота |
| `!watch on/off` | Мониторинг сессий |
| `!broadcast <текст>` | Рассылка по диалогам |

### Чат-команды
| Команда | Описание |
|---------|----------|
| `!admins` | Список администраторов |
| `!bots` | Список ботов |
| `!promote @user` | Назначить администратором |
| `!demote @user` | Снять администратора |
| `!zombie` | Очистка удалённых аккаунтов |
| `!react` | Случайная реакция |
| `!dice` | Бросок кубика |
| `!avatar [@user]` | Аватар пользователя |
| `!mention` | Упомянуть всех |
| `!web <url>` | Открыть веб-страницу |
| `!protect` | Защитить группу |
| `!unprotect` | Снять защиту группы |
| `!leave` | Выйти из группы |
| `!invite <ссылка>` | Пригласить по ссылке |
| `!invite all` | Пригласить всех участников |
| `!purge [количество]` | Удалить сообщения |
| `!purgeall` | Удалить все сообщения |
| `!hentai` | Hentai-команда |

### 🎭 RP-команды

Работают **только в личных сообщениях**. Для отправки RP-действия:

- **Любой пользователь** в ЛС — пишет команду, бот отвечает
- **Owner/Sudo** — реплай на чьё-то сообщение с командой (RP от имени бота к этому человеку)

Более 90 RP-действий: `обнять`, `поцеловать`, `ударить`, `погладить` и т.д.

## 🔧 Зависимости

- `telethon` — работа с Telegram API
- `yt-dlp` — загрузка YouTube (с обходом блокировок)
- `requests` / `aiohttp` — HTTP-запросы
- `flask` — веб-статус

## 📁 Структура

```
├── main.py          # Основной код бота
├── rp_commands.py   # RP-команды (словари)
├── storage.py       # Хранилище (Storage)
├── requirements.txt # Зависимости
└── media/           # Папка загрузок
```
